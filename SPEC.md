# 花のつくり暗記ゲーム 仕様書

外部ライブラリに依存しない、単一の静的Webページ（PWA対応）。理科「花のつくり」
単元（がく・花びら・おしべ・めしべの数、虫媒花/風媒花、花粉の形、単子葉/双子葉の
分類）を、ストーリー・クイズ・達成度管理で覚えるための学習アプリ。

- 公開URL: https://soyomin.github.io/hana-quiz/
- リポジトリ: https://github.com/soyomin/hana-quiz

## 1. ファイル構成

```
hana-quiz/
├── index.html        アプリ本体（HTML+CSS+JSを1ファイルに内包）
├── manifest.json      PWAマニフェスト（アプリ名・アイコン・テーマカラー）
├── sw.js              Service Worker（オフラインキャッシュ）
├── icons/             ホーム画面用アイコン（192/512/512-maskable/favicon）
├── README.md          使い方の簡単な説明
└── SPEC.md            本ドキュメント
```

`index.html` は外部フォント・外部JSライブラリを一切使わない自己完結ファイル。
グラフィック（花の模式図・花粉の形）はすべてJSで動的生成するSVG。

## 2. データモデル（index.html 内 `<script>`）

### FLOWERS（8種）
数字テスト・単子葉クイズ・まとめテストの元データ。

```js
{ name, fam, sect, sepal, petal, stamen, pistil, note, skipQuiz?, display? }
```

- `sect`: `"単子葉類"` | `"双子葉類"`
- `skipQuiz`: 数値では出題しない項目キーの配列（例: タンポポの `["sepal"]`）
- `display`: `skipQuiz` 対象の代替表示文字列（例: タンポポの がく＝`"多数（冠毛）"`）

| 花 | 科 | 分類 | がく | 花びら | おしべ | めしべ | 備考 |
|---|---|---|---|---|---|---|---|
| ジャガイモ | ナス科 | 双子葉類 | 5 | 5 | 5 | 1 | |
| アブラナ | アブラナ科 | 双子葉類 | 4 | 4 | 6 | 1 | おしべ長4短2 |
| チューリップ | ユリ科 | 単子葉類 | 3 | 3 | 6 | 1 | がくも花びら状 |
| ハナショウブ | アヤメ科 | 単子葉類 | 3 | 3 | 3 | 1 | 柱頭3裂 |
| アサガオ | ヒルガオ科 | 双子葉類 | 5 | 5 | 5 | 1 | 合弁花 |
| エンドウ | マメ科 | 双子葉類 | 5 | 5 | 10 | 1 | 9+1 |
| サクラ | バラ科 | 双子葉類 | 5 | 5 | 30(目安/実際は多数) | 1 | |
| タンポポ | キク科 | 双子葉類 | 多数(冠毛)/クイズ対象外 | 5 | 5 | 1 | 頭状花の小花1つ分 |

### PARTS
`sepal / petal / stamen / pistil` の表示ラベル・単位・診断図の色を定義。

### pollinateExamples（虫媒花/風媒花クイズ、8件）
`{ name, type }`。type は `"虫媒花"` | `"風媒花"`。
虫媒花: アブラナ・サクラ・タンポポ・アサガオ／風媴花: イネ・トウモロコシ・スギ・マツ。

### POLLEN（花粉の形クイズ、8種）
`{ name, type, shape, desc }`。`shape` は `pollenSVG()` が解釈する模式図の種類。

| 花 | 分類 | 形 |
|---|---|---|
| ヒマワリ | 虫媒花 | spiky-long（太く長いとげ） |
| カボチャ | 虫媒花 | spiky-dense（短いとげが密生） |
| アブラナ | 虫媒花 | ridged-oval（俵形＋すじ模様） |
| ユリ | 虫媒花 | ridged-oval-long（細長いだ円＋すじ模様） |
| ツツジ | 虫媒花 | tetrad（4つがつながり、粘着糸あり） |
| トウモロコシ | 風媒花 | smooth（なめらかな球形） |
| マツ | 風媒花 | two-sacs（空気袋が2つ） |
| スギ | 風媒花 | one-bump（突起が1つ） |

## 3. 画面構成（タブ）

1. **🕵️ なぜその数？**（デフォルト表示） — 11の物語（アブラナ／チューリップ／
   ジャガイモ／タンポポ／サクラ／エンドウ／アサガオ／マツ／スギ／カボチャ／
   ヘチマ）をトグルで切り替え。続けて「単子葉＝3の合言葉／双子葉＝4か5の
   合言葉」という分類ルールと、変則的な数字（エンドウ9+1、タンポポの冠毛、
   サクラの多数）の個別の理由を掲載。フリップカード形式の花図鑑タブ（旧
   「①おぼえる」）は廃止し、この物語タブに一本化した。
2. **🔍 単子葉クイズ** — 花を見て単子葉類/双子葉類を当てる2択クイズ
3. **② 数字テスト** — 4要素×8種（skipQuiz除く）から4択出題。花ごとに出題対象を
   ON/OFFできるトグルあり
4. **③ 虫媒花・風媒花** — 特徴比較の説明のみ
5. **④ 虫媒花クイズ** — pollinateExamplesから2択出題
6. **⑤ 花粉の形** — 特徴比較＋8種のギャラリー（説明のみ）
7. **⑥ 花粉クイズ** — POLLENの模式図から花の名前を4択で当てる
8. **🏆 まとめテスト** — 数字/単子葉/虫媴花風媴花/花粉をランダム20問ミックス、
   正答率に応じた称号表示
9. **📊 達成度** — 総解答数・正答率・苦手項目数・最高連続正解数のサマリー、
   苦手ランキング、カテゴリ別の達成度バー、記録リセットボタン

②④⑥🔍の各クイズには「🔁 苦手だけ出題」トグルがあり、`isWeak()` の条件を
満たす項目だけに絞って出題できる。

## 4. 達成度・統計（localStorage）

- キー: `hana-stats-v1`（JSON）
- 統計の粒度別キー命名: `num|花名|部位キー` / `pol|花名` / `poln|花名` / `sect|花名`
- 値: `{ c: 正解数, w: 不正解数 }`
- `isWeak(key)`: `(c+w)>0 && w>0 && w>=c` の項目を「苦手」と判定
- 「記録をリセット」ボタンは2回押し確認方式（ネイティブ`confirm()`は使わない）

## 5. サウンド

`Web Audio API`（`AudioContext` + `OscillatorNode`）ですべて動的生成。外部音源なし。

- `playCorrect()`: 3音の上昇アルペジオ
- `playWrong()`: 2段階の低いブザー音
- `playTap()`: タブ切替・カードめくり用の短いクリック音
- `playFanfare(pct)`: 各クイズ終了時、正答率に応じて音色を変える

応援メッセージ（`PRAISE` / `CONSOLE_MSG`）は `pickVaried()` で直前と同じ文言が
連続しないようにランダム選択。3の倍数・5の倍数の連続正解で `streakNote()` が
専用メッセージを返す。

## 6. デザイン

- カラーパレット: 薄い水色（`--accent`）＋白ベース、花びら＝ピンク（`--petal`）、
  がく＝ミント（`--sage`）、おしべ＝黄色（`--accent-2`）、めしべ＝紺（`--pistil`）
- 背景: 水色×白の直線ギンガムチェック（`linear-gradient` 2枚重ねのフラットな
  市松、45度回転はNG＝炭治郎の柄に見えるため不採用）
- ダークモード: `prefers-color-scheme` と `data-theme` 属性の両対応
- 角丸を強めた「かわいい」トーン、タイトル両脇にリボン絵文字装飾

## 7. PWA / デプロイ

- `manifest.json`: `display: standalone`、テーマカラー`#8FCBEA`
- `sw.js`: install時に主要ファイルをキャッシュ、fetch時はキャッシュ優先＋
  バックグラウンド更新
- GitHubアカウント `soyomin` で管理。コミットは
  `300474778+soyomin@users.noreply.github.com`（本名非公開）のみを使用する運用
- GitHub Pages（`main`ブランチ / ルート）で公開。リポジトリの「About」欄にも
  公開URLを設定済み

## 8. 既知の制約・注意点

- サクラのおしべは教科書上「多数（約20〜40本）」が正しいが、本アプリでは
  目安として固定値「30」を正解にしている（ユーザー了承済みの仕様）。
- 花粉の形はSVGによる模式図であり、実物の顕微鏡写真とは見た目の再現度が
  完全ではない。
